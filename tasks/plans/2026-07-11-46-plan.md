# Issue #46 — Small Robustness Batch: Implementation Plan

**Goal:** Four independent, small robustness fixes: scoped cache deletion, score rounding, date-parse warning, and a shared `vintageLabel()` helper used by all vintage-rendering tools.

**Architecture:** No new files. `config.ts`'s `clearUserData` gains a filename allowlist sourced from `exporter.ts`'s existing `TABLES` map (single source of truth, no duplicated table list). `query.ts` gains a module-level dedup `Set` for date-parse warnings and a new exported `vintageLabel(row)` helper. `server.ts`'s `formatScores` becomes exported and rounds numeric values to 1 decimal; six call sites across `fmtWine`/5 tool handlers switch from ad-hoc `row.Vintage ?? "NV"` to `vintageLabel(row)`.

---

### Task 1: Scoped cache deletion (AC a)

**Files:**
- Modify: `src/config.ts:150-167` (`clearUserData`'s cache branch)
- Test: `src/__tests__/config.test.ts` (new case in the `clearUserData` describe block)

- [ ] Write failing test: a non-CSV / unrelated file in the cache dir survives `clearUserData({ cache: true })`, while a real table file (`List_latest.csv`) is removed.
- [ ] Run `npm test -- config.test.ts`, confirm it fails (extra file gets deleted today).
- [ ] Import `TABLES` from `./exporter.js` in `config.ts`; filter `files` to only those starting with `${tableName}_` and ending `.csv` for some `tableName` in `Object.keys(TABLES)` before unlinking.
- [ ] Run test again, confirm PASS. Run full `npm test`, confirm no regressions in existing `clearUserData` cases (they only ever write `<Table>_latest.csv` names, so still match).
- [ ] Commit.

### Task 2: Score rounding (AC b)

**Files:**
- Modify: `src/server.ts:35-45` (`formatScores`) — add `export`.
- Test: `src/__tests__/server.test.ts` (new describe block, direct import — pure function, no mocking needed)

- [ ] Write failing test importing `formatScores` from `../server.js` with a row like `{ CT: "91.9254658385093", WA: "92.0833333333333" }`, expect `"CT:91.9, WA:92.1"`.
- [ ] Run test, confirm it fails (currently returns raw floats).
- [ ] Update `formatScores`: keep the existing raw-value zero-filter (`"0"`/`"0.0"` literal skip), then parse the surviving value with `parseFloat` and format via `Math.round(num * 10) / 10`, falling back to the raw string if `parseFloat` is `NaN`. Add `export` to the function signature.
- [ ] Run test, confirm PASS. Also assert a whole-number score like `"92"` still renders `"CT:92"` (no forced trailing `.0`), and `"0"`/`"0.0"` are still excluded.
- [ ] Commit.

### Task 3: Date-parse warning (AC c)

**Files:**
- Modify: `src/query.ts:97-117` (`toIsoDate`)
- Test: `src/__tests__/query.test.ts` (extend the `toIsoDate` describe block)

- [ ] Write failing tests: (1) an unparseable non-empty value logs exactly one `console.error` call even when passed twice; (2) empty/undefined input never calls `console.error`. Use `vi.spyOn(console, "error").mockImplementation(() => {})` and restore in `afterEach`/at test end. Use a unique nonsense string per test to avoid cross-test dedup collisions (the warned-set is module-level/process-lifetime).
- [ ] Run test, confirm it fails (no warning exists today).
- [ ] Add a module-level `const warnedDateFormats = new Set<string>();` above `toIsoDate`. In the final `return "";` fallback path (non-empty, unparseable), if `!warnedDateFormats.has(v)`, add it and `console.error(...)` a one-line warning naming the offending value.
- [ ] Run test, confirm PASS. Run full `toIsoDate` suite, confirm no output-shape regressions (return values unchanged).
- [ ] Commit.

### Task 4: `vintageLabel()` extraction (AC d)

**Files:**
- Modify: `src/query.ts` (new exported function, near `toIsoDate`)
- Modify: `src/server.ts` — 6 call sites: `fmtWine` (~line 50), `drinking-recommendations` handler (~line 220), `get-wishlist` handler (~line 463), `consumption-history` handler (~line 520), `tasting-notes` handler (~line 585), `recent-deliveries` handler (~line 419)
- Test: `src/__tests__/query.test.ts` (new describe block for `vintageLabel`)

- [ ] Write failing test: `vintageLabel({ Vintage: "1001" })` → `"NV"`; `vintageLabel({ Vintage: "" })` → `"NV"`; `vintageLabel({})` → `"NV"`; `vintageLabel({ Vintage: "2015" })` → `"2015"`.
- [ ] Run test, confirm it fails (function doesn't exist yet).
- [ ] Add `export function vintageLabel(row: Row): string { const v = (row.Vintage ?? "").trim(); return !v || v === "1001" ? "NV" : v; }` to `query.ts`.
- [ ] Run test, confirm PASS.
- [ ] In `server.ts`, import `vintageLabel` from `./query.js` and replace all 6 raw `row.Vintage ?? "NV"` / `r.Vintage && r.Vintage !== "1001" ? ... : "NV"` expressions with `vintageLabel(row)` (or `vintageLabel(r)` in recent-deliveries).
- [ ] Run full `npm test`, confirm no regressions (existing tests don't assert on raw `"1001"` rendering, so nothing should break).
- [ ] Manually grep `src/server.ts` for `?? "NV"` and `!== "1001"` to confirm zero remaining raw vintage-sentinel handling outside `vintageLabel`.
- [ ] Commit.

### Task 5: Full verification

- [ ] Run `npm test` (full suite) — expect 0 failures.
- [ ] Run `npm run build` — expect clean `tsc` compile.

---

**Verification commands:**
- `npm test` — full vitest suite, expect all green.
- `npm run build` — `tsc`, expect exit 0.
